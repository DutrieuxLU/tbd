use rusqlite::Connection;

pub fn tbd_add(conn: &Connection, command_args: &[String]) {
    dbg!(command_args);
}
pub fn tbd_adjust(conn: &Connection, command_args: &[String]) {
    dbg!(command_args);
}
pub fn tbd_complete(conn: &Connection, command_args: &[String]) {
    dbg!(command_args);
}
pub fn tbd_help(conn: &Connection, command_args: &[String]) {
    dbg!(command_args);
}
pub fn tbd_list(conn: &Connection, command_args: &[String]) {
    dbg!(command_args);
}
